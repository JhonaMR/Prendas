# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================

# Puerto donde correrá el servidor
# Los clientes se conectarán a http://IP_SERVIDOR:3000
PORT=3000

# Entorno de ejecución (development o production)
NODE_ENV=development

# ============================================
# SEGURIDAD - JWT (JSON Web Tokens)
# ============================================

# Secreto para firmar los tokens de autenticación
# ⚠️ IMPORTANTE: Cambia esto en producción
# Para generar uno seguro, usa: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=123456

# Tiempo de expiración del token (ejemplos: 1h, 24h, 7d)
JWT_EXPIRES_IN=24h

# ============================================
# BASE DE DATOS
# ============================================

# Ruta donde se guardará la base de datos SQLite
# Por defecto se crea en ./database/inventory.db
DATABASE_PATH=./database/inventory.db

# ============================================
# CORS - Control de Acceso (Navegadores)
# ============================================

# Orígenes permitidos para hacer peticiones al backend
# En desarrollo (frontend en Vite): http://localhost:5173
# En producción (frontend en el mismo servidor): http://IP_SERVIDOR:3000
# Puedes poner varios separados por coma
CORS_ORIGIN=http://localhost:5173,http://localhost:3000,http://10.10.0.34:5173,http://10.10.0.34:3000

# ============================================
# RED LOCAL (Para producción)
# ============================================

# IP del servidor (0.0.0.0 escucha en todas las interfaces)
# Esto permite que otros PCs de la red accedan al backend
HOST=0.0.0.0

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. Este archivo es un EJEMPLO - Copia este archivo como .env
# 2. NUNCA subas el archivo .env real a Git (ya está en .gitignore)
# 3. Cambia JWT_SECRET por uno único y seguro
# 4. En producción, cambia CORS_ORIGIN a la IP real de tu servidor
#    Ejemplo: http://192.168.1.100:3000
#
